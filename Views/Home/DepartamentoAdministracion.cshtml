@using System.Data;
@model DataTable;
@{
    ViewData["Title"] = "Administración";
}

<div class="text-center">
  <h1 class="LetraGrande">Listado de empleados</h1>
</div>
<div class="text-center">
  <div class="container; caja">
    <div class="row">
      <div class="col-md-5">
        <h2 class="titulo">Nombre del empleado</h2>
      </div>
      <div class="col-md-7">
        <h2 class="titulo">Datos y descripción</h2>
      </div>
      <div class="text-center">
      @foreach (DataRow r in Model.Rows)
            {
              <div class="container">
                <div class="row"><hr>
                  <div class="col-md-5">
                    <h3 class="lisgrup">
                        @Convert.ToString(r["NombreEmpleado"])
                    </h3>
                  </div> 
                  <div class="col-md-7">
                    <ul class="list-group">
                      <li class="list-group-item lisgrup"><span><p>Nombre:</p></span>@Convert.ToString(r["NombreEmpleado"])
                      <li class="list-group-item lisgrup"><span><p>Tareas:</p></span>@Convert.ToString(r["TareaNombre"])
                      <br>
                    </ul>
                  </div>  
                </div>
              </div>
            }
        </div> 
      </div>
    </div>
  </div>
</div>